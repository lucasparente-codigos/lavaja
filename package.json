{
  "name": "lavaja-mono",
  "private": true,
  "scripts": {
  "dev": "concurrently \"yarn server\" \"yarn client\"",
  "server": "cd backend && yarn dev",
  "client": "cd frontend && yarn dev"
},
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}
